{
  "mcpServers": {
    "{{SERVER_NAME}}": {
      "command": "{{COMMAND}}",
      "args": ["{{ARG_1}}", "{{ARG_2}}"],
      "env": {
        "{{ENV_VAR}}": "{{ENV_VALUE}}"
      },
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
